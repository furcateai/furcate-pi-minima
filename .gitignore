/target
**/*.rs.bk
Cargo.lock.user

# Local operator artefacts that should never be committed.
# `furcate-minima init` materialises a chain dir + secrets at FURCATE_MINIMA_ROOT.
/var/
/etc/furcate-minima/
*.jar

# Claude Code — no Claude state in public repos
.claude/
CLAUDE.md
CLAUDE.local.md

# macOS
.DS_Store
